import styled from "@emotion/styled"

const Content = styled('div')(() => ({
    display: 'flex',
    alignItems: 'center',
    flexDirection:'column',
    height: 'auto',
    width:'100%',
    top:'100px',
}))

export default Content